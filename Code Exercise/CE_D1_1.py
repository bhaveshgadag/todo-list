# 1. Create a variable (any name) and store a string to it.
# 2. Then, print out the type of the variable.

name = input("Enter name: ")
print("Your name is ", name, " and stored in variable name, which is of type : ", type(name))
